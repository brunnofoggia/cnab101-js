export enum DIRECTION {
    LEFT = 'LEFT',
    RIGHT = 'RIGHT',
    L = 'LEFT',
    R = 'RIGHT',
}

export enum COLUMN_DIRECTION_PAD_METHOD {
    LEFT = 'padEnd',
    RIGHT = 'padStart',
}

export enum COLUMN_DIRECTION_FILL {
    LEFT = ' ',
    RIGHT = '0',
}
